name: Run my script

on: [push]

jobs:
  run-script-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Run the script
        run: |
          chmod +x ./scripts/my-script.sh # Make script executable
          ./scripts/my-script.sh
